// Preload script for safe context isolation
// Expose any native APIs here if needed via contextBridge.
window.addEventListener('DOMContentLoaded', () => {
  console.log('Distribuidora M-H Desktop Loaded');
});
